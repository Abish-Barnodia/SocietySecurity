{
  "name": "apartment-security",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:api": "npm run dev --prefix apps/api-server",
    "dev:resident": "npm run start --prefix apps/resident-app",
    "test:api": "npm run test --prefix apps/api-server"
  }
}
